[qtalk]

exten => 100,1,Dial(PJSIP/100,30)
 same => n,Hangup()

exten => 101,1,Dial(PJSIP/101,30)
 same => n,Hangup()

; Эхо-тест: сервер возвращает принятый звук вызывающему абоненту.
exten => 600,1,Answer()
 same => n,Echo()
 same => n,Hangup()
